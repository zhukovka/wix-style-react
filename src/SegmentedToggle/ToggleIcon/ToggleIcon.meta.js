import ToggleIcon from './ToggleIcon';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ToggleIcon);

metadata.exportInfo = {
  path: 'src/SegmentedToggle/ToggleIcon/ToggleIcon.js',
  exportName: 'ToggleIcon',
};
