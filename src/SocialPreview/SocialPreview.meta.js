import SocialPreview from './SocialPreview';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SocialPreview);

metadata.exportedFrom({
  path: 'src/SocialPreview/SocialPreview.js',
});
