import GooglePreview from './GooglePreview';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(GooglePreview);

metadata.exportedFrom({
  path: 'src/GooglePreview/GooglePreview.js',
});
