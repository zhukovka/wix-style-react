import LinearProgressBarNew from './LinearProgressBarNew';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(LinearProgressBarNew);

metadata.exportedFrom({
  path: 'src/LinearProgressBarNew/LinearProgressBarNew.js',
});
