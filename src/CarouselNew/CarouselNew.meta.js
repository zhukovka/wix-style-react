import CarouselNew from './CarouselNew';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CarouselNew);

metadata.exportedFrom({
  path: 'src/CarouselNew/CarouselNew.js',
});
